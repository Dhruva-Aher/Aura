{
  "version": 2,
  "buildCommand": "cd apps/operator-console && npx vite build",
  "outputDirectory": "apps/operator-console/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
